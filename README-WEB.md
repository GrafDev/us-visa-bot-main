# US Visa Bot - Web Manager 🚀

Веб-интерфейс для управления несколькими клиентами US Visa Bot одновременно.

## Особенности

- 🎯 **Управление множеством клиентов** - добавляйте и управляйте неограниченным количеством клиентов
- 🖥️ **Современный веб-интерфейс** - React + Vite для быстрой и отзывчивой работы
- 📊 **Логи в реальном времени** - WebSocket для мгновенного отображения логов
- 💾 **Локальное хранилище** - все данные хранятся в SQLite на вашем компьютере
- ▶️ **Простое управление** - запускайте и останавливайте ботов одним кликом
- 📝 **Удобное редактирование** - модальные окна для управления данными клиентов

## Установка

### 1. Установите зависимости

```bash
npm run install:all
```

Эта команда установит зависимости для сервера и клиента.

### 2. Запустите приложение

```bash
npm run dev
```

Это запустит:
- Backend сервер на `http://localhost:3001`
- Frontend приложение на `http://localhost:3000`

### 3. Откройте браузер

Перейдите на `http://localhost:3000`

## Использование

### Добавление клиента

1. Нажмите кнопку **"➕ Add Client"**
2. Заполните все обязательные поля:
   - **Name** - имя клиента (для вашего удобства)
   - **Email** - email для входа на ais.usvisa-info.com
   - **Password** - пароль
   - **Country Code** - код страны (например: `ru`, `fr`, `de`)
   - **Schedule ID** - ID расписания (из URL при переносе даты)
   - **Facility ID** - ID консульства
   - **Current Date** - текущая забронированная дата
   - **Target Date** (опционально) - целевая дата (бот остановится при достижении)
   - **Min Date** (опционально) - минимальная приемлемая дата
3. Нажмите **"Create"**

### Запуск бота

Нажмите зеленую кнопку **"▶ START"** в строке клиента. Кнопка станет красной **"⏹ STOP"**, что означает что бот запущен.

### Остановка бота

Нажмите красную кнопку **"⏹ STOP"** чтобы остановить бота.

### Просмотр логов

Кликните на последний лог в колонке **"Last Log"** чтобы открыть панель с полными логами для клиента.

### Редактирование клиента

1. Нажмите кнопку **"✏️"** в строке клиента
2. Внесите изменения
3. Нажмите **"Update"**

⚠️ **Важно**: Нельзя редактировать клиента пока бот запущен. Сначала остановите бота.

### Удаление клиента

1. Нажмите кнопку **"🗑️"** в строке клиента
2. Подтвердите удаление

⚠️ **Важно**: Нельзя удалить клиента пока бот запущен. Сначала остановите бота.

## Архитектура

```
├── server/               # Backend (Node.js + Express)
│   ├── db/              # SQLite база данных
│   ├── routes/          # REST API endpoints
│   ├── services/        # Bot Manager, логирование
│   └── server.js        # Главный сервер
├── client/              # Frontend (React + Vite)
│   └── src/
│       ├── components/  # React компоненты
│       ├── services/    # API клиент
│       └── utils/       # Утилиты
└── src/                 # Оригинальный код бота
```

## API Endpoints

- `GET /api/clients` - получить всех клиентов
- `GET /api/clients/:id` - получить клиента
- `POST /api/clients` - создать клиента
- `PUT /api/clients/:id` - обновить клиента
- `DELETE /api/clients/:id` - удалить клиента
- `POST /api/clients/:id/start` - запустить бота
- `POST /api/clients/:id/stop` - остановить бота
- `GET /api/clients/:id/logs` - получить логи

## WebSocket

WebSocket подключение на `ws://localhost:3001` предоставляет real-time обновления:

- **initial_status** - начальный статус всех ботов
- **status** - обновление статуса бота
- **log** - новая запись в логах

## База данных

Все данные хранятся в файле `server/db/clients.db` (SQLite).

Таблицы:
- **clients** - информация о клиентах
- **logs** - логи для каждого клиента

## Скрипты

- `npm run server` - запустить только backend
- `npm run client` - запустить только frontend
- `npm run dev` - запустить оба (рекомендуется)
- `npm run build` - собрать production версию frontend
- `npm run cli` - запустить оригинальный CLI бот
- `npm run install:all` - установить все зависимости

## Требования

- Node.js 16+
- npm или yarn

## Отличия от CLI версии

| CLI версия | Web версия |
|------------|------------|
| Один клиент за раз | Множество клиентов одновременно |
| Конфигурация через .env | Данные в базе данных |
| Логи в терминале | Логи в веб-интерфейсе |
| Запуск через командную строку | Управление через браузер |

## Безопасность

⚠️ **Важно**:
- Все пароли хранятся в открытом виде в базе данных
- База данных находится локально на вашем компьютере
- Не выкладывайте файл `clients.db` в публичный доступ
- Рекомендуется запускать только в локальной сети

## Troubleshooting

### Порт уже занят

Если порт 3001 или 3000 занят, измените их в:
- `server/server.js` (строка: `const PORT = process.env.PORT || 3001`)
- `client/vite.config.js` (в настройках proxy)

### WebSocket не подключается

Проверьте что backend сервер запущен и доступен на порту 3001.

### Бот не запускается

1. Проверьте логи в панели логов
2. Убедитесь что все данные клиента корректны
3. Проверьте что оригинальный код бота работает через CLI

## License

ISC License
