# Быстрый старт 🚀

## 🖥️ Desktop приложение (Рекомендуется!)

### Запуск Electron приложения (самый простой способ)

```bash
# 1. Установить зависимости (первый раз)
npm run install:all
npm install

# 2. Собрать frontend
npm run build

# 3. Запустить приложение
npm run electron
```

**Готово!** Приложение откроется в отдельном окне, сервер запустится автоматически.

### Сборка приложения для установки

```bash
# Для macOS (создаст .dmg и .zip)
npm run electron:build:mac

# Для Windows (создаст .exe установщик)
npm run electron:build:win

# Для Linux (создаст .AppImage и .deb)
npm run electron:build:linux
```

Собранное приложение будет в папке `dist-electron/`

📚 Подробнее: [ELECTRON.md](ELECTRON.md)

---

## 🌐 Веб-версия (через браузер)

### Установка (один раз)

```bash
# Установить зависимости для сервера и клиента
npm run install:all
```

### Запуск

```bash
# Запустить веб-приложение (сервер + клиент)
npm run dev
```

Приложение откроется на:
- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:3001

## Первое использование

1. Откройте браузер: http://localhost:3000
2. Нажмите "➕ Add Client"
3. Заполните форму с данными клиента
4. Нажмите "▶ START" чтобы запустить бота
5. Кликните на последний лог чтобы открыть панель с логами

## Структура данных

Все данные хранятся в файле:
```
server/db/clients.db
```

⚠️ **Важно**: Этот файл содержит пароли в открытом виде. Не делитесь им!

## Отдельные команды

```bash
# Только сервер
npm run server

# Только клиент
npm run client

# Собрать production версию
npm run build

# Запустить оригинальный CLI бот
npm run cli -- -c 2025-06-15
```

## Особенности

- ✅ Множество клиентов одновременно
- ✅ Логи в реальном времени
- ✅ Автоматическое переподключение WebSocket
- ✅ Удобное управление через веб-интерфейс
- ✅ Все данные локально

## Получение необходимых данных

### Country Code
Найдите в URL: `https://ais.usvisa-info.com/en-{COUNTRY_CODE}/`
Примеры: `ru`, `fr`, `de`, `br`

### Schedule ID
Найдите в URL при переносе даты:
`https://ais.usvisa-info.com/en-{COUNTRY}/niv/schedule/{SCHEDULE_ID}/continue_actions`

### Facility ID
Найдите в network-запросах при выборе даты, либо в HTML коде выпадающего списка с консульствами.

## Поддержка

Для деталей смотрите [README-WEB.md](README-WEB.md)
