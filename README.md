# US Visa Bot Manager 🤖

Desktop приложение для автоматического мониторинга и бронирования дат собеседования в посольстве США.

## 📥 Для пользователей

### Скачать готовое приложение

**macOS (Apple Silicon - M1/M2/M3)**
- [US Visa Bot Manager-1.2.0-arm64.dmg](dist-electron/US%20Visa%20Bot%20Manager-1.2.0-arm64.dmg) - Установщик (рекомендуется)
- [US Visa Bot Manager-1.2.0-arm64-mac.zip](dist-electron/US%20Visa%20Bot%20Manager-1.2.0-arm64-mac.zip) - Архив

**Windows (64-bit)**
- [US Visa Bot Manager Setup 1.2.0.exe](dist-electron/US%20Visa%20Bot%20Manager%20Setup%201.2.0.exe) - Установщик (рекомендуется)
- [US Visa Bot Manager 1.2.0.exe](dist-electron/US%20Visa%20Bot%20Manager%201.2.0.exe) - Portable версия

### Документация

- **[USER_GUIDE.md](USER_GUIDE.md)** - Подробное руководство пользователя
- **[RELEASE_NOTES_v1.1.0.md](RELEASE_NOTES_v1.1.0.md)** - Описание версии 1.1.0
- **[CHANGELOG.md](CHANGELOG.md)** - История изменений

## ✨ Возможности

- 🖥️ **Desktop приложение** - Работает на macOS и Windows
- 👥 **Управление несколькими клиентами** одновременно
- 🔄 **Автоматический мониторинг** доступных дат
- 📅 **Автоматическое бронирование** при нахождении подходящей даты
- 📋 **Логи в реальном времени** через WebSocket
- 💾 **Локальная база данных** - все данные на вашем компьютере
- 🎯 **Настраиваемые фильтры** - target date и minimum date
- 📊 **Status Bar** - статистика и индикация подключения

## 🛠 Для разработчиков

### Требования

- Node.js 18+ (тестировалось на v25.2.1)
- npm 9+

### Установка

```bash
# Клонировать репозиторий
git clone <repository-url>
cd us-visa-bot-main

# Установить зависимости
npm install
cd client && npm install && cd ..
```

### Режимы запуска

#### Dev режим (для разработки)

Запускает только веб-сервер без Electron оболочки. Открывает веб-интерфейс в браузере.

```bash
npm run dev
```

Затем откройте http://localhost:3001 в браузере.

**Преимущества:**
- Быстрый старт без пересборки Electron
- Легче отлаживать в браузере
- Нет проблем с Electron API в dev окружении

#### Production режим (Electron app)

Собирает и запускает полноценное Electron приложение.

```bash
npm start
```

**Примечание:** В текущей версии `npm start` может не работать из-за проблем с загрузкой Electron API в dev окружении. Используйте `npm run dev` для разработки или готовые сборки (.dmg/.exe) для продакшена.

### Сборка дистрибутивов

```bash
# Для macOS (Apple Silicon)
npm run pack:mac

# Для Windows x64
npm run pack:win

# Для Linux
npm run pack:linux
```

Собранные файлы появятся в папке `dist-electron/`.

### Структура проекта

```
us-visa-bot-main/
├── client/               # React frontend (Vite)
│   ├── src/
│   │   ├── components/  # UI компоненты
│   │   ├── services/    # API клиент
│   │   └── main.jsx     # Entry point
│   └── package.json
├── server/              # Express backend
│   ├── db/             # SQLite база данных
│   └── server-electron.js  # Основной сервер
├── electron/           # Electron обертка
│   ├── main.js        # Electron main process
│   └── preload.js     # Preload script
├── src/
│   └── commands/      # Bot worker scripts
└── package.json       # Основной package.json
```

### Технологический стек

- **Frontend:** React 18 + Vite
- **Backend:** Node.js + Express + WebSocket
- **Database:** SQLite (better-sqlite3)
- **Desktop:** Electron (для готовых сборок)
- **Process Management:** Child processes для изоляции ботов

### Скрипты

```bash
npm run dev          # Запуск dev сервера (без Electron)
npm start            # Запуск production сборки (с Electron)
npm run build        # Сборка React frontend
npm run pack:mac     # Сборка .dmg для macOS
npm run pack:win     # Сборка .exe для Windows
npm run pack:linux   # Сборка для Linux
```

## ⚠️ Известные проблемы

### Electron не запускается в dev режиме

**Проблема:** `require('electron')` возвращает путь к executable вместо API модуля в dev окружении.

**Решение:** Используйте `npm run dev` для разработки (запускает сервер без Electron) или готовые сборки (.dmg/.exe) для тестирования desktop функционала.

**Почему так:** Electron API доступен только внутри Electron runtime. В некоторых окружениях Node.js не может корректно загрузить Electron модуль до запуска самого Electron процесса.

## 🔧 Разработка

### Отладка

1. **Frontend:** Запустите `npm run dev` и откройте DevTools в браузере
2. **Backend:** Логи выводятся в консоль сервера
3. **Electron:** Собранные приложения (.dmg/.exe) работают корректно

### База данных

База данных SQLite находится в `server/db/clients.db`.

**Резервное копирование:**
```bash
cp server/db/clients.db server/db/clients.db.backup
```

**Просмотр:**
```bash
sqlite3 server/db/clients.db
.tables
SELECT * FROM clients;
```

## License

This project is licensed under the ISC License.

## Disclaimer

This bot is for educational purposes. Use responsibly and in accordance with the terms of service of the visa appointment system. The authors are not responsible for any misuse or consequences.
