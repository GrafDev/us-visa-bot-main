# 🎉 Electron Desktop App - Готов!

## Что было добавлено

Теперь US Visa Bot Manager - это полноценное **desktop приложение** для macOS, Windows и Linux!

### ✅ Реализовано

1. **Electron Main Process** ([electron/main.js](electron/main.js))
   - Создает окно приложения
   - Автоматически запускает Express сервер
   - Управляет жизненным циклом
   - Graceful shutdown

2. **CommonJS Server** ([server/server-electron.js](server/server-electron.js))
   - Версия сервера совместимая с Electron
   - Весь функционал оригинального сервера
   - Автоматически запускается при старте приложения

3. **Preload Script** ([electron/preload.js](electron/preload.js))
   - Безопасный мост между процессами
   - Context isolation

4. **Build Configuration**
   - Electron Builder настроен для macOS, Windows, Linux
   - Автоматическое создание установщиков
   - Иконки (опционально)

## 🚀 Как использовать

### Для разработки

```bash
# 1. Установить все зависимости
npm run install:all
npm install

# 2. Собрать frontend
npm run build

# 3. Запустить Electron
npm run electron
```

### Для конечного пользователя (сборка)

```bash
# Собрать для macOS
npm run electron:build:mac
```

Результат в `dist-electron/`:
- `US Visa Bot Manager-1.0.0.dmg` - готовый установщик для macOS
- `US Visa Bot Manager-1.0.0-mac.zip` - zip архив

Просто отправьте .dmg файл пользователю!

## 🎯 Преимущества Desktop версии

| Web версия | Desktop версия |
|-----------|----------------|
| Запуск через терминал | Двойной клик |
| Нужен браузер | Встроенный |
| Сервер запускается отдельно | Автоматически |
| `npm run dev` | Иконка в Dock/Taskbar |

## 📦 Структура

```
us-visa-bot-main/
├── electron/
│   ├── main.js              ← Главный процесс Electron
│   └── preload.js           ← Preload для безопасности
├── server/
│   ├── server.js            ← Оригинальный (ES6 модули)
│   └── server-electron.js   ← Для Electron (CommonJS)
├── client/dist/             ← Собранный React app
└── dist-electron/           ← Собранные приложения (.dmg, .exe)
```

## 🔧 Технические детали

### Как это работает

```
┌──────────────────────────────────────────┐
│   Electron Main Process (main.js)       │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │  BrowserWindow                     │ │
│  │  Загружает: client/dist/index.html│ │
│  │  Подключается к: localhost:3001   │ │
│  └────────────────────────────────────┘ │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │  Child Process: server-electron.js │ │
│  │  - Express API (port 3001)        │ │
│  │  - WebSocket server               │ │
│  │  - SQLite database                │ │
│  │  - Bot Manager                    │ │
│  └────────────────────────────────────┘ │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │  Bot Workers (child processes)     │ │
│  │  - Client 1 bot                   │ │
│  │  - Client 2 bot                   │ │
│  │  - ...                            │ │
│  └────────────────────────────────────┘ │
└──────────────────────────────────────────┘
```

### Жизненный цикл

1. **Пользователь запускает приложение**
   → Electron Main Process стартует

2. **Main Process запускает сервер**
   → Спавнит child process с server-electron.js
   → Ждет 2 секунды для инициализации

3. **Main Process создает окно**
   → Загружает client/dist/index.html
   → React app подключается к localhost:3001

4. **Пользователь работает с приложением**
   → Все как в веб-версии

5. **Пользователь закрывает приложение**
   → Main Process останавливает сервер (SIGTERM)
   → Сервер останавливает все боты
   → Graceful shutdown

## 🎨 Кастомизация

### Иконки (опционально)

Для красивого приложения создайте:

**macOS:**
- Файл: `assets/icon.icns`
- Размер: 1024x1024 px
- Формат: .icns

**Windows:**
- Файл: `assets/icon.ico`
- Размер: 256x256 px
- Формат: .ico

Без иконок будет использоваться стандартная иконка Electron.

### Название приложения

Изменить в [package.json](package.json):
```json
"build": {
  "productName": "Your App Name",
  "appId": "com.yourcompany.app"
}
```

## 📝 Скрипты

| Команда | Описание |
|---------|----------|
| `npm run electron` | Запустить приложение |
| `npm run electron:dev` | Запустить в dev режиме (с DevTools) |
| `npm run electron:build:mac` | Собрать для macOS |
| `npm run electron:build:win` | Собрать для Windows |
| `npm run electron:build:linux` | Собрать для Linux |
| `npm run electron:build` | Собрать для всех платформ |

## ⚠️ Важные моменты

1. **Перед запуском `npm run electron`**:
   - Обязательно запустите `npm run build` для сборки frontend
   - Иначе увидите пустое окно

2. **База данных**:
   - Создается в `server/db/clients.db`
   - Сохраняется между запусками
   - Переносима (можно скопировать на другой компьютер)

3. **Сборка для Windows на Mac**:
   - Требует Wine
   - Или можно собрать на Windows машине

4. **Размер приложения**:
   - macOS: ~200-300 MB (включает Chromium)
   - Это нормально для Electron приложений

## 🐛 Troubleshooting

### Пустое окно при запуске
**Проблема**: Окно открывается, но пустое

**Решение**:
```bash
npm run build
npm run electron
```

### Ошибка "Cannot find module"
**Проблема**: Сервер не может найти модули

**Решение**:
```bash
npm install
npm run electron
```

### База данных не сохраняется
**Проблема**: Данные теряются при перезапуске

**Решение**: Проверьте что путь `server/db/` существует

## 🎉 Готово к распространению!

После сборки (`npm run electron:build:mac`):

1. Найдите файл в `dist-electron/US Visa Bot Manager-1.0.0.dmg`
2. Отправьте его пользователю
3. Пользователь устанавливает как обычное приложение
4. Запуск - двойной клик!

**Никаких терминалов, никаких npm команд - просто работающее приложение!** 🚀

## 📚 Дополнительно

- Полная документация: [ELECTRON.md](ELECTRON.md)
- Быстрый старт: [QUICKSTART.md](QUICKSTART.md)
- Веб-версия: [README-WEB.md](README-WEB.md)
