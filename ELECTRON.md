# Electron Desktop App - US Visa Bot Manager

## Desktop приложение для macOS, Windows и Linux

Теперь US Visa Bot Manager доступен как полноценное desktop приложение! Просто двойной клик и приложение готово к работе.

## 🚀 Запуск для разработки

### 1. Установите зависимости (первый раз)
```bash
npm run install:all
npm install
```

### 2. Соберите frontend
```bash
npm run build
```

### 3. Запустите Electron приложение
```bash
npm run electron
```

## 📦 Сборка приложения

### Для macOS (на Mac)
```bash
npm run electron:build:mac
```

Создаст:
- `dist-electron/US Visa Bot Manager-1.0.0.dmg` - установщик для macOS
- `dist-electron/US Visa Bot Manager-1.0.0-mac.zip` - zip архив

### Для Windows (нужен Wine на Mac или запуск на Windows)
```bash
npm run electron:build:win
```

Создаст:
- `dist-electron/US Visa Bot Manager Setup 1.0.0.exe` - установщик
- `dist-electron/US Visa Bot Manager 1.0.0.exe` - portable версия

### Для Linux
```bash
npm run electron:build:linux
```

Создаст:
- `dist-electron/US Visa Bot Manager-1.0.0.AppImage` - универсальный пакет
- `dist-electron/us-visa-bot_1.0.0_amd64.deb` - пакет для Debian/Ubuntu

### Универсальная сборка для всех платформ
```bash
npm run electron:build
```

## 🎯 Как это работает

### Electron приложение включает:

1. **Electron Main Process** (`electron/main.js`)
   - Создает окно приложения
   - Запускает Express сервер автоматически
   - Управляет жизненным циклом приложения

2. **Express Server** (`server/server-electron.js`)
   - Запускается автоматически при старте приложения
   - REST API для управления клиентами
   - WebSocket для real-time обновлений
   - SQLite база данных

3. **React Frontend** (`client/dist/`)
   - Загружается в Electron окне
   - Подключается к локальному серверу
   - WebSocket для живых обновлений

### Архитектура

```
┌─────────────────────────────────────────┐
│      Electron Main Process              │
│  ┌───────────────────────────────────┐  │
│  │  BrowserWindow (React App)        │  │
│  │  http://localhost:3001            │  │
│  └───────────────────────────────────┘  │
│                                          │
│  ┌───────────────────────────────────┐  │
│  │  Express Server (Child Process)   │  │
│  │  - REST API                       │  │
│  │  - WebSocket                      │  │
│  │  - SQLite DB                      │  │
│  │  - Bot Manager                    │  │
│  └───────────────────────────────────┘  │
│                                          │
│  ┌───────────────────────────────────┐  │
│  │  Bot Workers (Child Processes)    │  │
│  │  - Bot 1 for Client 1             │  │
│  │  - Bot 2 for Client 2             │  │
│  │  - ...                            │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

## 🔧 Настройка иконок

Для красивого приложения нужны иконки:

### macOS (.icns)
1. Создайте иконку 1024x1024 px
2. Конвертируйте в .icns формат
3. Сохраните как `assets/icon.icns`

### Windows (.ico)
1. Создайте иконку 256x256 px
2. Конвертируйте в .ico формат
3. Сохраните как `assets/icon.ico`

Без иконок приложение будет использовать стандартную иконку Electron.

## 📁 Структура файлов

```
us-visa-bot-main/
├── electron/
│   ├── main.js          # Main Electron process
│   └── preload.js       # Preload script
├── server/
│   ├── server-electron.js   # Server для Electron (CommonJS)
│   └── server.js            # Оригинальный server (ES6)
├── client/dist/         # Собранный React app
├── assets/
│   ├── icon.icns        # macOS иконка
│   └── icon.ico         # Windows иконка
└── dist-electron/       # Собранные приложения
```

## 🎨 Особенности

### ✅ Что работает
- Автоматический запуск сервера при старте приложения
- Graceful shutdown - все боты корректно останавливаются
- База данных сохраняется между запусками
- Все функции веб-версии доступны
- Native меню и окна

### ⚡ Преимущества Desktop версии
- Не нужно запускать терминал
- Двойной клик для запуска
- Нативное приложение в Dock/Taskbar
- Автоматический запуск сервера
- Полная изоляция (не нужен браузер)

## 🔒 Безопасность

Приложение использует:
- `contextIsolation: true` - изоляция контекста
- `nodeIntegration: false` - отключение Node.js в renderer
- Preload script для безопасного доступа к API

## 🐛 Отладка

### Режим разработки
```bash
npm run electron:dev
```

В этом режиме:
- Откроются DevTools
- Загружается Vite dev server (нужно запустить отдельно)
- Hot reload для быстрой разработки

### Логи
Логи Electron отображаются в терминале где вы запустили приложение.

## 📝 Различия версий

| Характеристика | Web версия | Electron версия |
|----------------|------------|-----------------|
| Запуск | `npm run dev` | Двойной клик .app/.exe |
| Сервер | Запускается отдельно | Автоматически |
| Браузер | Нужен | Встроенный |
| Установка | Не требуется | .dmg/.exe |
| Обновления | git pull | Новая версия приложения |
| Платформы | Любая с браузером | macOS/Windows/Linux |

## 🚨 Важно

1. **Перед сборкой** всегда запускайте `npm run build` чтобы обновить frontend
2. **База данных** создается в `server/db/clients.db` и сохраняется между запусками
3. **Иконки** опциональны но делают приложение красивее
4. **Сборка для Windows на Mac** требует Wine или можно собрать на Windows машине

## 📚 Полезные команды

```bash
# Разработка
npm run electron:dev              # Запуск в dev режиме

# Сборка
npm run build                     # Собрать frontend
npm run electron:build:mac        # Собрать для macOS
npm run electron:build:win        # Собрать для Windows
npm run electron:build:linux      # Собрать для Linux
npm run electron:build            # Собрать для всех платформ

# Запуск
npm run electron                  # Запустить приложение
```

## 🎉 Готово!

После сборки у вас будет готовое к установке приложение в папке `dist-electron/`.

Просто отправьте .dmg (macOS) или .exe (Windows) файл пользователю, и он сможет установить приложение как любое другое!
